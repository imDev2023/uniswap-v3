# Octopus

**Octopus** is a DEX + bonding-curve launchpad on **Robinhood Chain** (chainID 4663). New projects launch via a pump.fun-style bonding curve; on reaching a fixed-ETH threshold a token graduates atomically into a permanently-locked, full-range `TOKEN/WETH` pool.

> **Naming.** "Octopus" is the product brand (working name). The AMM underneath is **unmodified Uniswap V3**, deployed byte-for-byte from the audited artifacts via `vm.getCode` (decision #4) — we run our own instance and own the factory, but we did not write an AMM.
>
> Keep the two separate when editing: **rebrand product surfaces** (UI copy, package names, subgraph deploy name, docs) — but **never rename or strip** technical/legal references to upstream: `@uniswap/v3-core`/`v3-periphery` deps, `IUniswapV3Factory`/`IUniswapV3Pool` interfaces, artifact paths in `V3Deployer.sol`, or the `GPL-2.0-or-later` SPDX headers. Those name real upstream software and carry licence obligations. The repo directory and git remote are still `uniswap-v3`; renaming those is cosmetic and deferred.
>
> Also unchanged: **deployed Solidity contract names** (`LaunchpadFactory`, `GraduationManager`, `LPLock`). Renaming them means redeploying and re-verifying the whole testnet stack and re-cutting the subgraph ABIs — not worth it pre-mainnet. The `Factory` entity id in the subgraph is likewise still the literal string `"launchpad"`.

The architecture decisions driving the build are charted on the **wayfinder map** — GitHub issue [#1](https://github.com/imDev2023/uniswap-v3/issues/1) (label `wayfinder:map`); its closed child tickets record each locked decision.

## Agent skills

### Issue tracker

Issues are tracked in this repo's **GitHub Issues** via the `gh` CLI. External PRs are **not** a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical label vocabulary — `needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout: a `CONTEXT-MAP.md` at the root points to per-context `CONTEXT.md` files (created lazily by `/domain-modeling`). See `docs/agents/domain.md`.

## Build workflow (for the next session)

The contracts live in `contracts/` (Foundry). Toolchain is installed at `~/.foundry/bin` and OpenZeppelin + Uniswap V3 artifacts are in `contracts/node_modules` (both persist on disk).

```bash
export PATH="$HOME/.foundry/bin:$PATH"
cd contracts && forge test          # 84 tests; fork tests need RPC_*_ARCHIVE_URL in contracts/.env
```

⚠️ **As of #32 every fork test is PINNED and forks from an archive endpoint** (`contracts/test/ForkConfig.sol`, aliases `robinhood_archive` / `robinhood_testnet_archive`). `contracts/.env` already has the URLs; `contracts/.env.example` documents them. Without them the fork suites fail loudly rather than falling back to a pruning endpoint.

Ticket rhythm (build tickets #12-#21 built the stack; #22-#28 are merged; **Stages 1 and 2 are done, and Stage 3 is UNDERWAY. `build/29-token-swap-pages` is MERGED to `main`; `build/30-metadata-range-bundle` is REVIEWED, its findings APPLIED, and MERGED to `main` (2026-07-31, fast-forward). `build/31-indexer-reorg-recovery` (Stage 4) is MERGED to `main` (2026-08-01, fast-forward, NOT pushed); `build/32-alchemy-endpoint-wiring` (Stage 4) is BUILT and awaiting review/merge**; remaining work is the rest of Stage 3 (metadata read side) or more of Stage 4 - and as of 2026-07-30 that work is the critical path, since the audit was moved to **after** project completion and the user's own testing pass. Tracked as GitHub issues; the map is #1, spec is #11 - note #26, #27, #28 and #29 have no issues of their own, they were scoped in-session):
1. One ticket per branch: `build/<NN>-<slug>`, branched from `main`.
2. Implement + write tests at the fork-test seam; keep the full suite green.
3. Run `/code-review` (two axes) against `main`; apply worthwhile findings.
4. Comment status on the ticket; merge the branch to `main` before the next ticket.

### Live testnet 46630 addresses (build #24 — supersedes everything earlier)

| | |
| --- | --- |
| `LaunchpadFactory` | `0x632FD8713356aCc4ec9BdC6b378c05707bc9D1E7` ✅ verified |
| `GraduationManager` | `0x3e28d8838951C9F1ad229a5506584616E46D5E14` ✅ verified |
| `LPLock` | `0x8FBAa12EEF6BB15C7dD33cCaAB62dbb9e3BeC0e1` ✅ verified |
| `UniswapV3Factory` | `0x158a14f6Aa8C86921e624e3ed0526F31520cB2BD` |
| `SwapRouter` | `0x4507B2864CEcaBE10330d927c9608AA55A00fFD3` |
| `NonfungiblePositionManager` | `0xFc1C035Dc7e0C91ECFE8AC3bC31D1AC05d780CC4` |
| `QuoterV2` | `0xfcfA720Fe7397cA75233C6DB7aCBDa5859835cf6` |
| `WETH9` (pre-existing) | `0x7943e237c7F95DA44E0301572D358911207852Fa` |
| Deployer = SAFE = treasury | `0x8Ec5f1e04531416d337E61733DfC5d1685D9A80C` |
| Subgraph `startBlock` | `94091260` |

`owner() == SAFE`, `pendingOwner() == 0x0`. This factory holds **14 launches, 2 of them graduated** (verified on-chain 2026-08-01: `launchCount() == 14`, matching the subgraph) - `META` was the original validation launch, 11 more were seeded on 2026-07-30 by `SeedTestnet.s.sol`, and `ORICH` + `OPIN` were added in #30 as metadata fixtures so the live board (#28) had real density to be designed against. Full table with addresses and progress in `docs/deployments-testnet.md`; the two graduated ones are **`META`** (token `0x52eEF29c3c869B4D04F3c1451b16548DEAA923bE`, pool `0xDC27FeCB8589c0FB0328fd98963c823a1681E933`, LP NFT 1) and **`SEND`** (token `0xF92A5Cd2F903750B83CF507897E5c5768Fc50Ebb`, pool `0x65F3aC73CeE1e0cF7130AA4b7974633Aff465D3f`). **The six older launches (`P1ETH`, `SNIPE`, `GRAD`, `RDOGE`, `SMOKE`, `ONEETH`) are on the SUPERSEDED factory `0xE98B99ADD42c550bf40B887Bf07A8f0119a22232` and are unreachable from the current one** - note the seeded set reuses the ticker `RDOGE`, at a different address.

**Testnet curve calibration:** the live 46630 factory graduates a new launch for **exactly 0.1 ETH** (`virtualEthReserve = 1/30 ETH = 33333333333333333`; ETH-to-graduate is always `3 × V_eth`), anti-snipe off — re-applied after the #24 redeploy via `setCurveParams(33333333333333333, 100, 8e24, 0)`. `setCurveParams` is future-only, so older launches keep their frozen calibration. Solidity constants are untouched — a **mainnet deploy still lands on production calibration (90 ETH)**. Restore/re-scale commands in `docs/deployments-testnet.md`.

**Current state:** #12-#25 done and merged to `main` - **the full build sequence is complete**, and the stack is **deployed and validated on testnet 46630**. Build **#24** (Stage 1: frozen curve params in `LaunchCreated`, on-chain `metadataURI`, `token`-indexed trade events, optimizer on) and build **#25** (Stage 2: RPC-first trading) are merged. Builds **#26** (Stage 4, mainnet RPC probe), **#27** (Stage 4, RPC failover + provider survey) and **#28** (Stage 3, the live board) are all merged to `main` (all fast-forward, 2026-07-30). Build **#29** (Stage 3, token page + swap page) was reviewed on both axes, had its findings applied, and was **merged to `main` 2026-07-31** (fast-forward). ✅ **`main` was PUSHED to `origin/main` on 2026-07-31** (`6635365`, fast-forward) - the long-standing 18-commit unpushed backlog is cleared and local and remote are in sync. Addresses, tx hashes and the full end-to-end smoke test are in **`docs/deployments-testnet.md`**; `subgraph/networks.json` and `frontend/.env.local` are wired to them. Contracts are feature-complete and **frozen for audit**; subgraph (#19) in `subgraph/`; frontend (#20/#21/#25) in `frontend/`.

**Suites, all green (re-verified on `build/32`, 2026-08-01):** 84/84 contracts (incl. fork tests, now pinned - and green from a **cold** RPC cache), 13/13 matchstick, **297/297 frontend**, `tsc -b` + `vite build` clean, bundle **653.65 kB / 204.33 kB gzip**. Also `node scripts/rpc-probe.mjs --self-test` **28/28** (was 18) and `node scripts/indexer-health.mjs --self-test` **19/19** (both offline, no deps).

Testnet run validated on-chain: curve create/buy/sell, the anti-snipe cap (including that a sell does **not** decrease `purchasedOf`), future-only `setCurveParams`, a full graduation (pool seeded + full-range LP NFT locked in `LPLock` + `feeProtocol` applied), and both swap-router directions incl. `multicall`+`unwrapWETH9`. **`P1ETH` graduated on the live 0.1 ETH calibration** (pool `0x8c723D40…CDf2`, LP NFT id 2 locked) and the whole chain reacted — subgraph `Graduation` entity matches wei-for-wei, frontend feed and swap page both light up. **`LPLock.collect` is proven on-chain**: fees swept to treasury from locked NFT 1 with principal and ownership untouched. `LaunchpadFactory`/`GraduationManager`/`LPLock` were Blockscout-verified on that deployment.

⚠️ **Build #24 redeployed the whole stack**, so every address above moved and the launches named in this paragraph (`P1ETH`, `SNIPE`, `GRAD`, `RDOGE`, `SMOKE`, `ONEETH`) live on the **superseded** factory `0xE98B99ADD42c550bf40B887Bf07A8f0119a22232`. Their validation records still stand for the behaviour they exercised, but they are not reachable from the current factory, and the contracts needed re-verifying on Blockscout. **All three are now verified** (`LaunchpadFactory`, `GraduationManager`, `LPLock` — solc 0.8.24, optimizer 200, confirmed in explorer metadata). The long-standing "internally-created contracts can't be verified" blocker is **resolved as of 2026-07-28**: the explorer now has creation bytecode for factory-created contracts, and both verified first try with ordinary `forge verify-contract` + explicit constructor args. See `docs/deployments-testnet.md`. The current deployment was re-validated end-to-end via launch `META` — see `docs/deployments-testnet.md`.

**The indexer is STOPPED as of 2026-07-30 - bring it up when you need it.** ⚠️ The user asked for it to be shut down between sessions because Docker was holding ~8 GB of host RAM. `cd subgraph/docker && docker compose up -d` restarts it; the `docker_graph-postgres` and `docker_graph-ipfs` volumes were preserved (`down` without `-v`), so it **resumes indexing where it left off - no re-sync from `startBlock`**. Give it ~60s to catch up before concluding anything is missing. ⚠️ Note most of that 8 GB is a SEPARATE `localai` / `n8n` / `open-webui` / `qdrant` stack the user runs, not this project's three containers (~510 MB combined) - do not stop those, and do not quit Docker Desktop, without asking.

`subgraph/docker/docker-compose.yml` runs graph-node + Postgres + IPFS; the subgraph is deployed as `octopus/octopus` and synced from `startBlock` 94091260. Query endpoint **`http://localhost:8100/subgraphs/name/octopus/octopus`** (both path segments; host ports are remapped into the 81xx range because 8000/8080 collide with other local stacks — status API on 8130, deploy admin on 8120). The older `launchpad/launchpad` name still exists in the DB pointing at the same deployment; `octopus/octopus` is the one to use. **The stack needs ~1 GB of Docker VM headroom** — if IPFS shows `Restarting (137)` with `OOMKilled=false`, the VM is in global OOM, not the subgraph broken; see `subgraph/README.md`. Every indexed entity reconciles against the on-chain smoke test — table in `subgraph/README.md`. The frontend runs against it: feeds, curve charts, holder tables and the swap page's pool resolution all populate from live subgraph data. On a 0.3s-block chain the **block ingestor**, not the log scan, is the bottleneck — stock settings sit ~2 min behind head and never converge; the tuned compose env holds a stable ~40s. Note `synced: true` compares against graph-node's *own* ingested head, so it can read true while genuinely behind the chain — measure lag against `eth_blockNumber`.

**`QuoterV2` is deployed** at `0xfcfA720Fe7397cA75233C6DB7aCBDa5859835cf6` (testnet 46630) via the standalone `contracts/script/DeployQuoter.s.sol` — a read-side lens with no owner and no funds, so it drops onto a live deployment safely. The swap page now shows **exact** quotes (fee + price impact) via `VITE_QUOTER_ADDRESS`, falling back to the old `slot0` estimate when unset. `contracts/test/QuoterV2.t.sol` pins quote == actual execution against the live pool. Note `quoteExactInputSingle` is non-`view` — call it with `eth_call` only.

**Multi-wallet testing is done.** `contracts/.env` holds `TEST_PK_1..6` (0.4 ETH each). Launch `SNIPE` exercised **anti-snipe across six competing wallets** — cap binding (`BuyCapExceeded`), cap-as-ceiling, sell-then-rebuy evasion blocked, the crossing buy still capped (decision #7), the cap lifting after the 120M window, and graduation with **6 holders** whose subgraph netting closes to the wei. Anti-snipe was armed for it and then **restored to the baseline** (`antiSnipeThreshold = 0`); re-arm with `setCurveParams(33333333333333333, 100, 25e24, 120e24)`. Note production (8M cap / 120M threshold) needs **≥15 distinct wallets** to traverse the window by design.

## Road to mainnet (agreed 2026-07-26; Stage 1 closed 2026-07-27, Stage 2 closed 2026-07-29; **sequencing revised 2026-07-30 — audit moved to LAST**)

**There is no backend left to build, and the contracts are now frozen.** What remains is the frontend redesign, deployment and hosting, and then the audit.

**Stages 1 and 2 are done.** Stages 3 and 4 are independent of each other; take them in either order.

🔴 **THE AUDIT NOW COMES LAST, AFTER THE PROJECT IS COMPLETE AND THE USER HAS TESTED IT HIMSELF. Do not ask about the audit, do not propose starting it, and do not treat it as a parallel track — until every remaining stage is finished.** Decided 2026-07-30, superseding the original plan (which was *hand to auditors → remaining stages in parallel with the audit wait*). The reason is social, not technical: the audit is a favour from the user's Solidity-developer friends, and the user will not spend their time on something they have not first driven end to end themselves and judged worth those friends' attention. The user will raise the audit when ready.

**Consequence for sequencing: remaining engineering is now the critical path in its own right, not something absorbed by an audit wait.** Audit plus remediation lands *after* Stages 3/4/5 rather than overlapping them, so the calendar is additive — do not present the remaining work as "free" schedule-wise. **Contracts nevertheless stay frozen:** the freeze was always about not redoing frontend work against a shifting data shape, and that reason is untouched by the resequencing.

### Stage 1 — finalise contracts ✅ DONE (build #24)

**Shipped and validated on testnet 46630.** `LaunchCreated` now carries the metadata URI and all six frozen curve params; `LaunchToken` has permanent `metadataURI` plus a `launchpad` back-reference; `token` is indexed on `Bought`/`Sold`; the optimizer is on. 84/84 contract tests, 13/13 matchstick, 43/43 frontend. **`docs/audit-scope.md` is the hand-off document for the auditors** — it emphasises the graduation transition and records one real finding: the crossing-buy clamp IS reachable, costing the protocol ≤1 wei while leaving the raise exactly calibrated.

What shipped, and the settled decisions behind it:

1. **`LaunchCreated` emits the full frozen curve set** — `metadataURI`, `virtualEthReserve`, `virtualTokenReserve`, `curveTokenAllocation`, `tradeFeeBps`, `maxBuyPerWallet`, `antiSnipeThreshold`. `createLaunch` builds ONE `CurveConfig` in memory and uses it both to construct the curve and to populate the event, so log and immutables cannot drift. Killed the `priceX18 = 0` bug (an untraded launch now indexes its true opening price) and removed the last hardcoded Solidity constant from the subgraph mappings.
2. **On-chain metadata URI** — `string public metadataURI` on `LaunchToken`, constructor-set, **no setter for anyone**. The three design decisions (all settled 2026-07-27):
   - ✅ **JSON metadata URI**, NFT-standard shape (`{name, description, image, banner, links}`) — not an image URI. Bought once so description/socials/banner can be added later without a new contract.
   - ✅ **Immutable at creation.** No setter, for anyone — matches the "fair launch, no rug" ethos the locked LP already establishes, and rules out bait-and-switch (clean art at launch, swapped after people buy).
   - ✅ **Stored on the token contract** (`string public metadataURI`) **and emitted** in `LaunchCreated`. Readable over plain RPC with no indexer, which is exactly what Stage 2 needs. ~40–45k gas once at creation, negligible against the 0.01 ETH creation fee.

   Shape: `createLaunch(name, symbol, metadataURI)`.

   ⚠️ **Consequence — this decision creates the project's first server-side component.** Immutable + content-addressed means uploading to IPFS *before* `createLaunch`, and pinning needs an API key that **cannot** live in a Vite bundle. So the create-token flow needs either (a) a small serverless upload/pin endpoint holding the pinning secret (Pinata / web3.storage / Filebase), (b) creators bringing their own URI (bad UX, fine for v1), or (c) a client-safe upload service. Decide during Stage 3. It also adds an **IPFS gateway** as a frontend read dependency — pick one deliberately and have a fallback.

   ⚠️ **Two consequences of immutability to design for:** an unpinned or typo'd URI is permanent, so the UI needs a graceful fallback avatar and the pin must be durable; and abusive imagery cannot be removed on-chain, so **moderation is a frontend denylist** — plan for one rather than discovering the need in production.
3. **Last-call pass.** Two additions accepted: **`token` indexed on `Bought`/`Sold`** (one `eth_getLogs` filter now covers every launch, including ones that don't exist yet — groundwork for Stage 2) and **`LaunchToken.launchpad`** (a token names its own factory, so `token.launchpad()` → `curveOf(token)` resolves a curve in two RPC calls with no indexer and no prior knowledge of the factory set). Two candidates **declined**: emitting cumulative `purchasedOf` in `Bought` (derivable), and changing the frozen `treasury` (documented as an owner operational constraint instead).
4. **Optimizer switched on (200 runs)** — it had never been enabled. `LaunchpadFactory` went 27,594 B → **15,942 B, under EIP-170**, so the deploy no longer depends on Orbit's relaxed limit.

**Audit is covered** — the user's Solidity-developer friends. **Do not propose audit vendors.** `docs/audit-scope.md` is the hand-off document: it directs reviewers at the **graduation transition** (atomic curve→pool handoff, crossing-buy refund arithmetic, curve rounding direction) — where our code meets Uniswap V3 and no upstream audit covers.

⚠️ **One real finding is already recorded there: the crossing-buy clamp IS reachable.** `grossNeeded` is a `ceilDiv` and the fee is a floor division, so `grossNeeded - 1` still crosses. The **raise stays exactly calibrated** — the protocol's own fee absorbs the wei — and underpaying by 1000 wei doesn't cross at all. Pinned in `contracts/test/CrossingBuyBoundary.t.sol`. The brief asks reviewers to verify the bound rather than re-derive it.

### Stage 2 — decouple trading from the indexer ✅ DONE (build #25)

**Shipped and validated against a real indexer outage.** The trade path no longer touches the subgraph. `hooks/useOnchainToken.ts` resolves everything from the one address baked into the build (`VITE_FACTORY_ADDRESS`): `curveOf(token)` → curve, `curve.graduated()`, `launchpad.v3Factory()` → `getPool(token, WETH, 10000)` → pool, plus ERC-20 `name`/`symbol`. Two Multicall3 rounds, verified on-chain to return exactly the addresses the subgraph used to supply.

Settled decisions:

1. **The launchpad is the root of trust, not `LaunchToken.launchpad()`.** A token can name any factory it likes; trusting that would let a hostile ERC-20 at `/token/0x…` point the UI at a fake curve and collect real ETH. Asking *our* launchpad "is this yours?" (`curveOf(token) != 0`) cannot be spoofed. Verified against WETH9 → "not a token launched on Octopus".
2. **Degradation is visible and per-panel.** A global banner states the indexer is down *and that trading is unaffected*; each indexed panel (chart / curve stats / holders) degrades to its own labelled notice rather than rendering empty — an empty chart reads as "no trades", a different and worse lie.
3. **Lag is measured in block timestamps, not `synced`.** graph-node's `synced` compares against its own ingested head and reads true while minutes behind. `_meta.block.timestamp` vs the RPC head timestamp is skew-free and needs no per-chain block-time constant. Threshold 300 s, matching the ops alert rule.
4. **Multicall3 declared on both chains** (canonical address, confirmed present on 4663 and 46630) so each resolution round is one `eth_call` at a single block — fewer round-trips and no torn read.

**Tested at the page level, not just in units.** `frontend/src/pages/SwapPage.test.tsx` renders the real page with every subgraph fetcher rejecting and asserts the pool still resolves and the panel still renders. It was mutation-checked: reintroducing `token.graduation.pool` fails 4 of its 5 tests. The pure resolvers (`lib/onchainToken.ts`, `lib/indexerHealth.ts`) carry the decision tables; the page test guards the *wiring*, which is what actually regresses.

⚠️ **Fixed a pre-existing trade-path bug:** viem's `isAddress` is strict about EIP-55 checksum casing, so a correctly-formed address with different casing was rejected as "Invalid token address" before any RPC call — including the META address as written in this file. Route params now parse case-insensitively via `lib/address.ts` and normalise with `getAddress`; the `curveOf != 0` check is what actually guards safety. A sweep of every `0x…` literal in `CLAUDE.md` and `docs/` through viem `getAddress` found **six mis-checksummed addresses**, all now corrected. Re-run that sweep after adding addresses by hand — strict tools (ethers v5 `getAddress`, some wallets) reject bad casing outright.

**Two Stage-2-adjacent items deliberately NOT done** (flagged, awaiting a decision — see *Open decisions*): an RPC fallback for the homepage token list, and RPC-derived curve progress on the token page.

### Stage 3 - frontend redesign ⬅️ IN PROGRESS (largest remaining chunk, and on the critical path)

**The three framing questions are ANSWERED (2026-07-30), and they bind the rest of the stage:**

1. ✅ **Ambition: re-think flows AND restyle.** Not a visual pass. Structural problems get fixed, not painted over. The 🐙 emoji wordmark **stays** as a placeholder - no commissioned identity for now.
2. ✅ **Direction: high-energy live board.** pump.fun-style - dense, kinetic, board-first, motion on new launches and trades, big progress meters, loud graduation moments. The restrained/credible alternative was considered and declined.
3. ✅ **Metadata: bring-your-own-URI plus strong fallbacks.** No serverless pin endpoint and no third-party upload service for v1. The read side still owes an IPFS gateway choice (with a fallback) and a moderation denylist; the fallback avatar is already built (#28).

**Agreed three-ticket split for the stage:**

| Ticket | Scope | State |
| --- | --- | --- |
| `build/28-live-board` | Testnet seed data · design-token system · price notation · homepage as a live board · empty + degraded states | ✅ **Merged to `main`** |
| `build/29-token-swap-pages` | Token page + swap page: real time-proportional chart (recharts → TradingView Lightweight Charts), merge the two overlapping right-rail cards, per-token trade feed, swap-page pool facts, trade/swap panel restyle | ✅ **Merged to `main`** |
| `build/30-metadata-range-bundle` | Metadata read side (IPFS gateway + denylist) · chart range selector · bundle trim | ✅ **Merged to `main`** - see the #30 shape below |

**Stage 2 changed the starting point for this work.** The pages split cleanly into an RPC-resolved trade path and indexer-derived analytics panels, with `OnchainTokenGate` owning every "nothing to trade here" state and `IndexerBanner` / `IndexedDataNotice` owning every degraded state. A redesign must preserve that split rather than re-entangle it - and the degraded states need visual design too, not just the happy path. #28 held to this and page-level tests now assert it.

### Stage 4 — infrastructure ⬅️ IN PROGRESS (now on the critical path)

**Build #31 closed the reorg-deadlock item** (diagnosis, recovery runbook, and `scripts/indexer-health.mjs`) and gives **monitoring** its first real probe - but monitoring is only *partly* done: the probe must still be wired to something that runs it and alerts. **Hosting, Postgres `C` collation, managed-host support for 4663, and 0.1 s-block tuning are all still untouched**, so the list below stands.

**Build #26 closed the one item that could have invalidated the whole approach: the mainnet RPC probe.** See the ✅ bullet below and `docs/rpc-capability.md`. **Build #27 then built the failover wiring and surveyed the provider field**, which reduces the "second endpoint" item to a single decision the user has to make: create an Alchemy account. Still fully open: **hosting** (frontend + indexer), **monitoring**, **Postgres `C` collation**, whether any managed subgraph host supports 4663 at all, and **indexer tuning for 0.1 s blocks** - which the probe flagged as the likeliest operational surprise in this stage and which nothing has touched yet.

**The chain is the source of truth; Postgres is a derived cache** rebuildable by re-indexing. So you need *fast re-index* and a *durable log source*, not PITR backups. Losing the indexer DB is an availability incident, not data loss. The one genuinely non-derivable thing is token metadata — which Stage 1 moves on-chain.

- **RPC is the real #1 dependency**, not the database: frontend *and* indexer both need it, and the frontend calls it **directly from users' browsers** (traffic scales with users; an API key in a Vite bundle is public — proxy it or use domain allowlisting). Run **two endpoints with failover**; it's the only component with no graceful degradation.
- ✅ **RESOLVED 2026-07-29 (build #26) - mainnet 4663 RPC is measured.** Full results in **`docs/rpc-capability.md`**; re-run any time with `node scripts/rpc-probe.mjs <mainnet|testnet|url>` (no deps, read-only, and it accepts any URL so it doubles as the provider-evaluation harness). **The "rebuildable cache" assumption HOLDS: mainnet serves `eth_getLogs` from block 0 with no pruning and no block-span cap** - proven with *unfiltered* windows at 11 depths down to block 0 (an address-filtered empty result would be ambiguous; an empty unfiltered window on a live chain is not).
- ⚠️ **Three mainnet findings that change Stage 4 work.** (1) **Blocks are 0.100 s**, ~3x faster than testnet, so the block-ingestor bottleneck that already needs tuning at 0.3 s gets ~3x worse - budget indexer tuning as real work, not a config copy. (2) **There are TWO log caps and the block span picks which applies: span ≤1000 allows 50,000 matched logs; span ≥1001 allows only 10,000.** The refusal text always says "limit of 10000", which is misleading. **Chunk log scans at span ≤1000** - it tolerates 5x the logs per request, so crossing to 1001 makes throughput *worse*. Neither cap limits the block span itself. (3) **Mainnet historical state is unreliable, not merely shallow** - see below.
- ⚠️ **Mainnet `eth_call` at depth is INTERMITTENT, because one URL fronts a pool of nodes with different retention.** Repeating the identical call 4x per depth: 5,000 blocks back is 4/4, but 10,000 is 3/4, 100,000 is 3/4, 5M is 2/4, 10M is 3/4, 20M is 0/4. Availability is **not monotonic**, so there is no boundary to find. **Only ~5,000 blocks (~9 min) is consistently served, so mainnet fork tests must stay inside that and read the head at runtime** (as `QuoterV2.t.sol` does). A deeper fork block passes locally and fails intermittently in CI. ⚠️ An earlier single binary search suggested "~21 days of state retention" - **that number was an artifact of sampling a load-balanced pool once, and is wrong.** Testnet is simpler: consistent to 5,000 blocks (~26 min), never beyond, no intermittency.
- ℹ️ Also measured: `fromBlock: "earliest"` is **rejected** on both chains (hex only - a trap for hand-written recovery scripts); filter/trace/`eth_syncing` methods are absent (graph-node polls `eth_getLogs`, so it is unaffected); Multicall3 is present on both with **identical bytecode hashes**, and WETH9 hashes identically across chains too, which substantiates the "byte-identical to mainnet" claim; head timestamps track wall clock to 1 s, supporting the Stage 2 lag measurement.
- ⚠️ **Three measurement traps, all of which produced confidently wrong readings before being caught** (recorded in `docs/rpc-capability.md`): `eth_getBalance` returns `0x0` with **no error** on unreadable state, so measuring archive depth with it reports "full archive" on a pruned node - **always use `eth_call`** (`missing trie node` / `metadata is not found`). A **single sample cannot measure a load-balanced endpoint** - repeat and report a rate, not a threshold. And a fixed-width log window blows the cap at every depth on a busy chain, so every row reads ERROR while looking tested - worse, a cap refusal *proves* logs exist there, so counting it as pruning inverts its meaning.
- ✅ **RESOLVED 2026-07-30 (build #27) - the failover WIRING is built, and the candidate field is surveyed.** The frontend now takes an ordered endpoint list (`VITE_RPC_URL`, `VITE_RPC_URL_2`, then the public endpoint as automatic last resort) through viem's `fallback` transport. **Adding a second endpoint is now a config change, not a code change.**
- ⚠️ **Still open, and it is an ACCOUNT decision, not an engineering one: there is no usable keyless second endpoint for 4663.** Measured, not assumed - NodeFlare's public endpoint works but allows **1 request per 10 s per IP** (its own page claims 2 req/s, so 20x stricter than documented); dRPC has the chain but paywalls it; thirdweb rejects 4663 outright; Ankr will not serve it; the `sequencer.` host is tx-submission only. Every viable provider (**Alchemy** - Robinhood's own recommendation - plus QuickNode, Blockdaemon, Validation Cloud) needs an account. Full table in `docs/rpc-capability.md`.
- ℹ️ **What failover actually buys is narrow, and was verified against viem rather than assumed.** The pruned-state errors are `-32000`, which is **not** in viem's retry set - so the measured intermittency **cannot be retried away against a single URL**, and a one-URL `fallback` is equivalent to a bare `http()` (viem forces `retryCount: 0` on inner transports). What `-32000` does do is leave `shouldThrow` false, so `fallback` advances to the next provider. A real `execution reverted` matches `shouldThrow` and does **not** cascade. Pinned in `frontend/src/lib/rpcFailover.test.ts`.
- ⚠️ Rate limits are still uncharacterised in shape (30 concurrent all succeed; sustained probing trips 429s). Note **repeated back-to-back probe runs trip the official limiter**, after which the adaptive pacing ratchets and a 2-minute run takes closer to an hour, silently. Space runs out.
- **Indexer hosting:** managed subgraph host (Goldsky / Alchemy / SettleMint — *ask whether they support 4663 first*) or self-host graph-node on a real VM (8 GB min, 16 GB comfortable). [Ponder](https://ponder.sh) is a lighter alternative (plain Postgres, no IPFS) but means rewriting the mappings and discarding 13 matchstick tests — don't switch unless graph-node ops become the problem.
- ⚠️ **Verify Postgres locale/collation before picking a managed provider.** graph-node requires `C` collation and it can only be set at cluster creation — unfixable afterwards without recreating the DB.
- **Frontend:** static SPA → Cloudflare Pages / Vercel / Netlify. Addresses are baked in at **build** time, so a contract change means a rebuild.
- **Monitoring:** graph-node already exposes Prometheus metrics (container 8040 → host 8140). Alert on **indexer lag > 5 min** (compare `latestBlock` to RPC `eth_blockNumber` — `synced: true` lies) and **any owner-role transaction**.
- **Rough cost ~$200–500/mo**: indexer VM $40–80, managed Postgres $25–100, RPC $100–300, frontend $0–20, monitoring $0–30.

**Service verdicts (asked 2026-07-26):** **Postgres** ✅ required (graph-node's store, not a choice). **PostHog** ✅ fine for analytics. **Convex** 🟡 good for metadata/comments/watchlists — must *not* replace the subgraph (no reorg handling, no re-index story). **Trigger.dev** 🟡 optional; one real use case is scheduled permissionless `LPLock.collect` fee sweeps, which otherwise accrue untouched. **Prisma** ❌ not needed — graph-node owns its Postgres schema (`sgd1.*`, block-range columns for time-travel); it is an internal format, not a stable API. Never write to it; query GraphQL instead.

### Stage 5 — mainnet deploy

Audit findings → fixes → deploy per `docs/deploy.md` (+ `DeployQuoter.s.sol`), remembering `WETH9=` explicitly. **Do not deploy to mainnet 4663 without asking the user first.**

### Open decisions (none block the next stage)

These are the user's calls, carried forward across sessions. Ask rather than assume.

- ✅ **SETTLED 2026-07-30 — the audit hand-off is deliberately NOT yet done, and is not a question to raise.** The user will complete the whole project, test it end to end themselves, and only then decide it is worth their Solidity-developer friends' time. `docs/audit-scope.md` stays written and waiting. **Do not ask about the audit again until every remaining stage is finished; do not propose vendors, ever.** See the resequencing note at the top of *Road to mainnet*.
- **Root `LICENSE` file** — the repo has none and declares none, while all Solidity is `GPL-2.0-or-later`. Choosing the project licence is the user's call.
- **"Octopus" trademark clearance** — not done.
- ✅ **SETTLED 2026-07-30 - frontend design direction.** Re-think flows AND restyle; high-energy live board; keep the 🐙 placeholder. See *Stage 3* above.
- ✅ **SETTLED 2026-07-30 - metadata approach: bring-your-own-URI with strong fallbacks.** No pin endpoint, no upload service for v1. ✅ **The two items left open inside it were settled in-session on 2026-07-31 and built in #30:** the **IPFS gateway** is `dweb.link` with a `w3s.link` fallback (different operators, on purpose), and the **moderation denylist** is a committed two-tier list that never blocks trading. The fallback avatar was done in #28. See the #30 shape above.
- **`SAFE` must become a REAL multisig before mainnet** — on testnet it is the deployer EOA.
- ✅ **SETTLED 2026-07-30 - `build/28-live-board` was merged to `main`** (fast-forward, no push), on the user's instruction.
- ✅ **SETTLED 2026-07-31 - `build/29-token-swap-pages` was reviewed on both axes, its findings applied, and it was merged to `main`** (fast-forward, no push), on the user's instruction.
- ✅ **SETTLED 2026-07-31 - `build/30-metadata-range-bundle` was reviewed on both axes, its findings applied, and it was merged to `main`** (fast-forward, **not pushed**), on the user's instruction. ⬅️ **`main` is now 2 commits AHEAD of `origin/main`.** Pushing is still a per-request action: ask.
- ✅ **SETTLED 2026-07-31 - `main` was pushed to `origin/main`** (`6635365`, fast-forward), clearing the 18-commit backlog that had accumulated across builds #22-#29. Local and remote are in sync. Pushing is still a per-request action, not a standing permission: **keep asking before you push.**
- ✅ **SETTLED 2026-08-01 - `build/31-indexer-reorg-recovery` was merged to `main`** (fast-forward, **not pushed**), on the user's instruction. ⬅️ **`main` is now 7 commits AHEAD of `origin/main`.** Pushing remains a per-request action: **ask.**
- ✅ **RESOLVED 2026-08-01 - the Alchemy account EXISTS and is measured.** `ALCHEMY_API_KEY` and `ENDPOINT_URL` are in `contracts/.env` (QuickNode creds too, but with **no endpoint URL**, so unusable). **One key serves both chains**: the `.env` URL is the `robinhood-testnet` subdomain (`eth_chainId` → `0xb626` = 46630) and swapping the subdomain to `robinhood-mainnet` returns `0x1237` = 4663. **It is a genuine archive node** - `launchCount()` returns real history 1.8M blocks deep on testnet and WETH9 `totalSupply()` works 20M deep on mainnet, where the public endpoint dies at 50,000 with `missing trie node`. ⚠️ **BUT it is on the FREE tier, which caps `eth_getLogs` to a 10-BLOCK RANGE on both chains** - graph-node scans in 1000-block ranges, so **Alchemy cannot back the indexer**. Full table in `docs/rpc-capability.md`. ⚠️ Still unmeasured: Alchemy's rate limits, and whether a paid tier lifts the log range.
- ✅ **ANSWERED 2026-08-01 by measurement, and WIRED in #32 - which key for the indexer vs the browser.** The two endpoints are **complementary, not interchangeable**, so the split is forced rather than chosen: **indexer → public endpoint** (needs wide `eth_getLogs`, makes **zero** `eth_call`s by design); **frontend → Alchemy primary, public as automatic fallback**; **fork tests → Alchemy** (needs archive state). All three are now in place. ⚠️ **The one thing this does NOT settle: the URL ships in the browser bundle and cannot hold a secret** - confirmed by grepping `dist/assets/index-*.js`, where the key appears verbatim. Fine locally (`.env.local` and `dist` are both gitignored); **domain allowlisting or a proxy is required before any public deploy.**
- ✅ **RESOLVED 2026-08-01 (build #32) - fork tests are pinned.** All six suites, not just `QuoterV2.t.sol`: four of them (`BondingCurve`, `V3Harness`, `LaunchpadFactory`, `OwnershipAndParams`) had been forking at `latest` and re-fetching live state every run, which is the likeliest source of the 8-failure flake. Pinned state caches to disk, so the whole 84-test suite now runs in ~1.2s offline.
- ~~**Which key to use for the indexer vs the browser?**~~ **Answered 2026-08-01 by measurement - see the resolved bullet above.** It is not a preference about traffic profiles any more: Alchemy's free tier caps `eth_getLogs` at 10 blocks, which rules it out for the indexer outright, while the public endpoint prunes `eth_call` state at ~5,000 blocks, which rules *it* out for archive reads. Each endpoint is the only viable option for its half.
- **Two Stage-2-adjacent items, scoped but not built** (deferred deliberately rather than dropped):
  - an **RPC fallback for the homepage token list** (`launchCount()` + `launches(i)`), so browsing survives an indexer outage too. Today the list degrades to a labelled note pointing at `/token/<address>`, which keeps trading reachable but not discovery.
  - **RPC-derived curve progress** on the token page (`curve.tokensSold()` / `curveTokenAllocation()`), so the graduation meter stays live during an outage. Both values are already in the `bondingCurve` ABI.
- ⚠️ **`docs/rpc-capability.md` contains two numbers that later re-runs contradicted, and recording the correction was declined.** Offered during #27 and NOT selected, so the doc still reads as first measured. Do not treat these as settled facts, and do not silently "fix" them either - re-offer instead:
  - it states **"only 5,000 blocks back is consistently served"**, but a second run a day later measured **10,000** and reshuffled which depths were intermittent (50,000 went 4/4 → 3/4). This *strengthens* the doc's actual conclusion - availability is a probability, not a boundary - and the conservative 5,000-block fork-test guidance should **stay** regardless. The number is stale; the guidance is not.
  - its method table says `eth_getBlockReceipts` is **NOT available**; two later runs reported it **available**.
  - Both are further evidence of the load-balanced pool. Re-run `node scripts/rpc-probe.mjs mainnet` to see for yourself, but space runs out (see the throttling warning in that doc).
- **Em-dash house style** — the user's global instructions say never use em dashes, but this repo's existing prose and UI copy use them throughout. Comments in files authored during #25 were converted to plain dashes; **existing copy was left alone** to avoid a typographically mixed product. A repo-wide sweep is available on request.

### Also settle before deploy

(The user's own calls — licence, trademark, real multisig — are in *Open decisions* above.)

- **Verify the V3 stack + QuoterV2 on Blockscout** — deployed via `vm.getCode`, so it needs a standard-JSON input built from the upstream `@uniswap/v3-core`/`v3-periphery` artifacts. Until then users cannot read the code they trade against.
- ⚠️ **Production-scale graduation (~90 ETH) has NEVER run on live testnet** - only in a fork test. Every on-chain graduation so far used the 0.1 ETH testnet calibration. The arithmetic is the same and the fork test passes, so this is a confidence gap rather than a known defect, but it is the largest untested path before real money touches it. Re-scaling is a `setCurveParams` call away (commands in `docs/deployments-testnet.md`) and needs ~90 test ETH to exercise.
- ⚠️ **Adversarial MEV is untested.** Anti-snipe was validated against six cooperating wallets, not against a searcher reordering or sandwiching curve buys on a 0.1 s-block chain. Nothing is scheduled for this.
- ✅ **RESOLVED 2026-08-01 (build #31) - the subgraph stall is DIAGNOSED, RECOVERED and DETECTABLE.** It was never a hang: it was a **crash-restart loop** caused by a **~134,300-block reorg** on testnet 46630, which left the deployment's stored head on an orphaned branch. graph-node cannot recover from this structurally - reverting a reorg means walking back through the orphaned blocks *by hash*, and no node serves non-canonical blocks. Recovered by purging the poisoned block cache and `graphman rewind`; re-indexing ~650k blocks took ~10 min and reconciles with the chain (`launchCount()` 14 == subgraph 14). Full runbook in `subgraph/README.md` → *Recovering from a reorg deadlock*; detect it with `node scripts/indexer-health.mjs`. See the #31 shape below.
- ⚠️ **Indexer tuning for mainnet's 0.1 s blocks is untouched**, and the #26 probe named it the likeliest operational surprise in Stage 4. Stock graph-node settings already never converge at testnet's 0.3 s; mainnet is ~3x worse. Budget it as real work, not a config copy.
- ✅ **RESOLVED 2026-07-28 — `GraduationManager` / `LPLock` are verified.** Both are internal `CREATE`s from the factory constructor, which the testnet explorer previously could not verify (no `creation_bytecode`). It now can: both verified first try with ordinary `forge verify-contract` + explicit constructor args. Mainnet Blockscout is a newer build (`v11.2.3` vs `v10.2.6`) and already hosts many verified factory-created contracts (18 of 20 sampled), so the gap does not exist there either. **The proposed fallback — deploying the two peripherals top-level, a contract change that would have had to precede the audit — is dropped. Contracts stay frozen as audited.** Details in `docs/deployments-testnet.md`.

ℹ️ **Fork-test flakiness against testnet 46630 was OBSERVED for the first time on 2026-07-31** (previously only predicted, and only for mainnet). One `forge test` run reported **8 failures**; three consecutive re-runs immediately after were **84/84**, with no code change in between. 8 is the size of a whole fork suite, so this read as one suite failing wholesale on an RPC hiccup rather than a real regression. ✅ **#32 addressed the mechanism**: every fork test now pins its block against an archive endpoint, so after the first fetch the suite reads from `~/.foundry/cache/rpc/` and makes no network call at all. That should retire this class of flake - but it has not yet been *observed* not to recur, so treat it as fixed-by-construction rather than proven. If an 8-failure run ever reappears, it is now a real signal, not weather.

✅ **RESOLVED 2026-08-01 (build #32) - the fork tests no longer touch the public endpoint, and the runtime-head dance is GONE.** All six fork suites pin their block and fork from the Alchemy archive endpoint via `contracts/test/ForkConfig.sol`. Everything in the next paragraph still describes the **public** endpoints accurately, and still governs anything that reads history over them - it just no longer governs `forge test`.

ℹ️ **What the public endpoints do, and why fork tests had to leave them.** Forking 46630 at `latest` fails: the node rejects state at the newest block (`-32000 unsupported block number`) and prunes state after ~5,600 blocks (~28 min). **Re-measured 2026-07-29 (#26):** state is consistently served only **5,000 blocks (~26 min)** back. A plain `eth_call` at `latest` did *not* reproduce the `unsupported block number` rejection, so that part looks specific to how forge forks rather than to bare RPC. ⚠️ **Mainnet 4663 is WORSE: only ~5,000 blocks (~9 min) is consistently served, and depths beyond that are *intermittent* because the endpoint is a pool of nodes with differing retention** - so a mainnet fork test at a deeper block passes locally and fails randomly. That pruning window is exactly why the old `QuoterV2.t.sol` read the head at runtime: a pinned constant rotted within the hour. **Alchemy answers 4/4 at every sampled depth to 20,000,000 blocks back**, so pinning became correct and the workaround became unnecessary. See `docs/rpc-capability.md`.

> ⚠️ **WETH9 is per-chain.** `Constants.WETH9` is **mainnet-only** and has **no code on 46630**. Testnet WETH9 is `0x7943e237c7F95DA44E0301572D358911207852Fa` (byte-identical proxy + implementation to mainnet's). Always pass `WETH9=` explicitly to `DeployLaunchpad.s.sol`. The frontend now selects it per `VITE_CHAIN_ID`.
>
> ℹ️ **The optimizer is on (200 runs) as of #24**, which took `LaunchpadFactory` from 27,594 B to **15,942 B — under EIP-170**. `--non-interactive` on `forge script --broadcast` is therefore no longer required (it existed only to skip forge's oversize TTY prompt, which died with `IO error: not a terminal`). Harmless to keep. Build settings are part of what the auditors review and what Blockscout verification must match.

**#32 shape (Build 19 - Stage 4, wire the Alchemy endpoint + pin the fork tests) - branch `build/32-alchemy-endpoint-wiring`.**

Config, not code, on the frontend side - #27 had already built the wiring. The contract-test side turned out to be bigger than the ticket assumed, and the probe turned out to be lying.

1. **Frontend → Alchemy primary.** `frontend/.env.local` sets `VITE_RPC_URL` to the Alchemy testnet URL; `rpcEndpoints.ts` appends the public endpoint automatically, so naming it would only create a duplicate. `VITE_RPC_URL_2` stays free for a third provider. Verified in the browser against **both** the dev server and the **production build**: board, token page, holders, chart and an exact QuoterV2 swap quote all resolve, with **every** RPC request going to Alchemy and **zero** to the public endpoint.
2. ✅ **Failover verified LIVE for the first time**, not just in unit tests. Aborting all traffic to the Alchemy host at the network layer made the production build fall straight through to the public endpoint (HTTP 200s) with the token page still rendering complete. #27's `rpcFailover.test.ts` pinned viem's *semantics*; this is the first time the configured list has actually been failed over.
3. ⚠️ **The fork-test problem was 6 suites, not 1.** The ticket named `QuoterV2.t.sol`, but four suites (`BondingCurve`, `V3Harness`, `LaunchpadFactory`, `OwnershipAndParams`) were forking at **`latest`** and re-fetching live state on every run - visible as **67 accumulated cache directories** under `~/.foundry/cache/rpc/4663/`, one per run. The two already-pinned suites (`Graduation`, `LpLock`) had been reading from a warm cache for weeks, which is why only some runs flaked. New `contracts/test/ForkConfig.sol` is the single place naming the endpoint and block; all six now pin. Suite is **84/84 from a cold cache in 3.9s**, and ~1.2s warm with no network at all.
4. **Archive access is the precondition for pinning, so it is enforced rather than optional.** New `robinhood_archive` / `robinhood_testnet_archive` aliases in `foundry.toml` read `RPC_*_ARCHIVE_URL` from `contracts/.env`. Missing them fails loudly (`environment variable ... not found`, verified by removing them) instead of silently falling back to a pruning endpoint - which is how the flakiness got in. New `contracts/.env.example` documents it; it had never existed, the same gap #27 fixed for the frontend.
5. ⚠️ **The probe misread Alchemy's free-tier cap as pruning - trap #3 in a new costume.** `isQueryTooBig` matched only *result-count* refusals; Alchemy refuses on *block range* (`up to a 10 block range`, `-32600`), so the span-shrinking loop never ran and the verdict read **`11 depth(s) failed for a NON-CAP reason`** - i.e. "this endpoint may be pruning logs" - about an endpoint serving logs from block 0. Fixed; the same endpoint now reports **`NO PRUNING`**. ⚠️ A follow-on: the first fix landed on span 2 (inclusive-range off-by-one), and at span 2 two windows came back **empty**, firing the "silently pruning?" warning. **A sampling window too small to contain a log manufactures the appearance of absence.** Self-test now covers this classifier as its own case table: **28 cases, up from 18**. Mutation-checked - dropping `block range` fails 3, narrowing the range parser fails 2.
6. ⚠️ **The probe also printed advice that contradicted its own table**: *"It is a MOVING window, so read the head at runtime rather than hardcoding a block number"*, printed unconditionally, directly beneath a table showing every depth answering 4/4. On an archive node that tells an operator to undo exactly the pinning this ticket introduced. Now derived from the reading. **The generalisable point: a measurement tool carrying fixed advice will eventually give wrong advice with the authority of a measurement.**
7. **Alchemy fully probed on both chains** (~280 calls, **zero 429s, zero timeouts**): archive `eth_call` **4/4 consistent at every sampled depth to 20,000,000 blocks back** on both, JSON-RPC batch supported, and `debug_traceTransaction` / `trace_block` / `eth_newFilter` **available** where the public endpoints have none. Mainnet block time confirmed at **0.100 s**. Full tables in `docs/rpc-capability.md`.
8. ⚠️ **The one thing that blocks shipping this config: the key is in the bundle.** Confirmed by grep - it appears verbatim in `dist/assets/index-*.js`. `.env.local` and `dist` are both gitignored so nothing leaked, but a public deploy needs **domain allowlisting or a proxy** first.

**#31 shape (Build 18 - Stage 4, indexer reorg diagnosis + recovery + health probe) - branch `build/31-indexer-reorg-recovery`.**

The stall carried in these docs since #30 is fully explained, fixed, and now detectable. **The headline is that every single health field graph-node exposes was green while the indexer was permanently dead**, which is the same class of lie as #30's `_meta.block.timestamp` bug and was found the same way - by looking at the running system, not the tests.

1. **Root cause: a ~134,300-block reorg, not a hang.** The deployment was in a **crash-restart loop**, logging `Subgraph instance failed to run: Ethereum node did not find block 0x91bc…eadb` from `TriggersAdapter::parent_ptr` every ~35 s. Comparing graph-node's own block cache against the chain block-by-block bounds the divergence to `(95175342, 95176342]` .. `95309661` - about **11 hours at 0.3 s blocks**, i.e. a testnet rollback, not an ordinary few-block reorg. Confirmed it is a reorg and not the #26 pruning/pool-intermittency: the stuck hash returns null 8/8 while by-hash lookups succeed at 400,000 blocks deep, and canonical 95309531 has a *different* hash. Not a chain wipe either - chainId still 46630, factory still has code, `startBlock` 94091260 still matches.
2. ⚠️ **graph-node cannot self-heal from this, structurally.** Reverting a reorg means walking back through the orphaned blocks via `parent_ptr`, which requires fetching them **by hash** - and no node serves non-canonical blocks. The recovery path depends on exactly the thing that is unavailable, so it retries forever.
3. ⚠️ **`synced` is worse than these docs claimed.** It is not "compared against graph-node's own ingested head" - it is a **sticky Postgres column** (`synced_at is not null`), set once when the deployment first reached head and **never re-evaluated. It cannot go false.** Confirmed by reading the table directly: `synced = t, health = healthy, failed = f, fatal_error = null` while 518,918 blocks behind. `health` only reflects mapping errors; `fatalError` only fires for **deterministic** errors, and a missing block is classified non-deterministic. All three are green during total failure.
4. ⚠️ **`graphman chain check-blocks` does NOT repair this**, despite being the tool whose stated job is "clears the block cache when they differ". Its repair path assumes it can re-fetch the block by hash, so on an orphaned block it **aborts** with `JRPC provider found no block with hash 0x…` instead of deleting the row. Recovery is a manual cache purge plus `graphman rewind`.
5. ⚠️ **Purging the poisoned cache is MANDATORY, not optional.** graph-node reads block metadata from its own cache, so rewinding without purging re-indexes the **wrong chain out of local storage**. 36,710 divergent rows were deleted before the rewind.
6. **Recovery worked and reconciles.** Rewound both deployments to canonical `95175342`; re-indexing ~650,000 blocks took **~10 minutes** (~1000 blocks/s). On-chain `launchCount()` is **14** and the subgraph reports **14** / 2 graduations. Board, token page, chart, trade feeds and holders all verified in the browser. `subgraph/docker/graphman.toml` is now committed (graphman refuses to start without `--config`, even read-only).
7. **New `scripts/indexer-health.mjs`** - dependency-free, strictly read-only, `--self-test` runs **19/19 offline**. It ignores `synced`/`health`/`fatalError` (reporting them only as untrustworthy context) and computes the two things that are true: **real lag** vs the chain's own `eth_blockNumber`, and **canonicity** - whether the stored head hash is the hash the chain has at that height. Canonicity is the decisive check, and `orphaned` deliberately **outranks** `lagging`, because an orphan is always also far behind and reporting the lag alone describes a permanent deadlock as a temporary delay. Exit code 0 only when everything is `ok`. Mutation-checked: reordering the orphan check below the lag check fails 2, dropping hash normalisation fails 1, dropping the null-guard throws on the case written for it.
   - ⚠️ **What it does NOT detect, stated so a green run is not over-read.** Canonicity catches the reorg deadlock - the crash loop that actually happened, and the only one that is permanent by construction. It does **not** catch a crash-restart loop from any other non-deterministic cause (flapping provider, OOMing container, a retry ladder stalling on a block that *is* canonical). Those present as a deployment that is merely behind, and the probe calls them `lagging`, which here is indistinguishable from healthy catch-up. Closing that gap needs restart counts or the log stream - graph-node's Prometheus endpoint (container 8040 → host 8140) is where they would come from, and that is **unbuilt Stage 4 work**. Until then, `lagging` that does not clear on its own deserves `docker compose logs graph-node`.
8. ⚠️ **Three defects in the probe were found by RUNNING it, and all three tests were green beforehand.** (a) The entry-point guard `import.meta.url === \`file://${process.argv[1]}\`` **silently ran nothing at all** - no error, no output - because this repo's path contains a space ("Work Folder") so `import.meta.url` is percent-encoded; fixed with `pathToFileURL`. (b) graph-node's status API returns hashes **without the `0x` prefix** while the RPC includes it, which would have reported every healthy deployment as `ORPHANED` - the loudest possible false alarm, on the one verdict that tells an operator to rewind a database. (c) **`indexingStatuses` with no arguments returns an EMPTY ARRAY** on graph-node 0.40.2 even with deployments assigned and actively indexing, while `indexingStatusesForSubgraphName` works - a monitor built on the obvious query reports "nothing to check" forever. The probe caught (c) on itself only because "no deployments" is an explicit `unknown`, never a vacuous pass.
9. ⚠️ **"The head number stopped advancing" is deliberately NOT a stall signal.** During a catch-up scan over ranges with no matching logs, graph-node legitimately leaves the stored head pointer untouched - measured during this recovery, it read `95175342` while the scanner was already past `95212453`. Alerting on non-advancement would fire loudest exactly when an operator is mid-recovery.

ℹ️ One frontend run reported **4 failures** (`findByText` timeouts) while the dev server was starting in parallel; 4 subsequent runs were **297/297**. CPU-contention flake, not reproducible.

**#30 shape (Build 17 - Stage 3/4, metadata read side + chart range selector + bundle trim) - MERGED to `main` 2026-07-31** (fast-forward, NOT pushed). Reviewed on both axes before merging; the review commit is `b4a1182`.

All three scoped items are done, and **two of the three found that the assumption in the ticket was wrong**, which is the theme worth carrying forward.

1. **Metadata read side.** Settled in-session: **`dweb.link` primary, `w3s.link` fallback** - deliberately *different operators*, because a fallback sharing an operator with its primary is not a fallback (`ipfs.io` and `dweb.link` are both Protocol Labs, which is why the two most famous gateways are not paired). New `lib/ipfs.ts` (URI → gateway URLs, path form so CIDv0 works, `URL`-normalised so `ipfs://cid/../../admin` cannot escape `/ipfs/`), `lib/tokenMetadata.ts` (whitelist parser; links narrowed to `https:` only, since that field becomes an `href`), `hooks/useTokenMetadata.ts`. Board reads the URI from the subgraph; token and swap pages read it over **RPC** (`useOnchainMetadataUri`), so identity survives an indexer outage like the rest of the Stage 2 trade path. The old per-browser `lib/tokenMeta.ts` localStorage map is **deleted**, along with the Create page's "Image URL" field whose own hint called it "a local preview until metadata rendering ships".
   - ⚠️ **The per-gateway timeout is the real engineering, not the gateway choice.** Measured against the seeded testnet CIDs: an unpinned CID hangs `dweb.link` for **28 seconds** before answering 504, while `w3s.link` refuses it in 0.13s. `GATEWAY_TIMEOUT_MS = 8000` bounds it; without it a board of forty cards sits on dozens of half-minute requests. `staleTime: Infinity` + `retry: false` follow from immutability - a CID addresses exactly one document, so a retry is guaranteed to fail again after another timeout.
   - ⚠️ **Testnet had NO working metadata case, and `META` is not one.** Nine of twelve launches carry no URI; `OCAT`/`BOOTS` are valid CIDs that were never pinned (504); and **`META`'s CID resolves 200 with a zero-byte body** - the case a naive `res.json()` throws on. Two launches were created to cover the happy path: **`ORICH`** `0x439F067FbCe73A6eB6e9e638B327370BF8c79D96` (a `data:` URI carrying description + links + a nested `ipfs://` image, so it depends on no gateway) and **`OPIN`** `0x5a3184C973d268Ab674F5b039F97e86Ee8456F84` (a real pinned `ipfs://…/1`).
2. **Moderation denylist** (`config/denylist.ts` + `lib/denylist.ts`), committed and compiled in - takedown is a commit plus a redeploy, which buys zero runtime dependencies for a project that has no server by design. Two tiers: `image` suppresses imagery, `hide` also removes the token from board, ticker and rails. ⚠️ **Neither tier blocks trading, deliberately** - someone may already hold a token we later hide, and refusing to render its trade panel would strand them in a position they cannot exit. Keyed by **address**, never symbol (attacker-chosen and duplicable) or CID (re-pinning escapes it).
3. **Chart range selector (1H/6H/1D/ALL).** `from` re-enters `buildPriceSeries` rather than being applied as a zoom, for the reason the #29 review established. New `lib/chartRange.ts`; `splitAtWindow` in `priceSeries.ts` carries in the price from the last trade *before* the window. ⚠️ **Two fabrication rules, both tested:** the line opens at the carried-in price (starting at the first in-window trade instead would crop the very step that trade caused, hiding a move by beginning already at the top of it); but it is **never stretched back to a window edge we have no price for** - a window opening before the oldest trade held starts at that trade, not at `from`. A **graduated** curve with no trades in the window renders empty rather than flat-to-now. The picker stays on screen in the empty state, or picking "1H" on a quiet curve would be a dead end with no way back to ALL. Picking a range also **releases the viewer's zoom claim** from #29 finding #2 - their pan claims the viewport, their range choice hands it back.
4. **Bundle trim: 704.36 → 653.62 kB, 217.62 → 204.28 kB gzip.** ⚠️ **The suspect named in the ticket was wrong.** The standing hypothesis - carried in these docs for several builds without being measured - was that wagmi drags in unused WalletConnect/MetaMask SDKs. The bundle contains **none of them**: only a couple of connector *name* strings, i.e. tree-shaking already working. Sourcemap attribution found the real cost: **`graphql` at 142 kB of source, 7.7% of the bundle** - the complete lexer, parser, visitor and printer - pulled in by `graphql-request`'s `analyzeDocument`, which parsed every query string into an AST **purely to read the operation name off it**, then discarded the AST and sent the original string. No option removes it (`excludeOperationName` skips the *call*, but the import is static). Replaced with `lib/graphqlClient.ts`, ~40 lines of `fetch`; `graphql-request` is uninstalled. **Method note: measure by sourcemap attribution, not by hypothesis** - `python3` over `sourcesContent`, grouped by `node_modules/<pkg>`.

⚠️ **A pre-existing bug made the ENTIRE Stage 2 degradation system inert, and it was found by running the app, not by tests.** graph-node 0.40.2 returns `_meta.block.timestamp: **null**` while `number` and `hash` are populated. A missing indexed timestamp classifies as `unknown` - which is also the first-load state, and which every surface stays deliberately quiet about. So an indexer **11 hours behind** showed **no banner at all**, while each indexed panel rendered its EMPTY state: a token page said *"No trades yet - be the first to buy this curve"* for a curve that had already traded four times, under a pulsing "Live" dot.

- **Why it was missed:** the Stage 2 work was validated by **stopping** graph-node, which makes it unreachable and exercises the `down` path. **`stale` - reachable but behind, the likelier production failure and the one the 5-minute ops alert is written for - had never once fired against a real indexer.**
- **Fix** (`resolveIndexedTimestamp` / `needsBlockTimestampLookup` in `lib/indexerHealth.ts`): read the indexed block's timestamp from RPC by number when graph-node withholds it. This *preserves* the original decision - lag is still the difference between two **chain** timestamps, with no per-chain block-time constant - and only changes the source of one of them. Block headers are never pruned (unlike state), so it resolves at any depth.
- **A second, independent liar in the same area:** both `TradeRail` (board) and `TokenTradeFeed` (token page) consulted indexer health **only on their error branch**. But a lagging indexer does not error - it answers successfully with an **empty array** for a token it has not reached, so the error branch never runs and "no rows" silently became the assertion "nobody has traded". Both now check health on the empty branch too, and both drop the pulsing live dot while degraded.
- ⚠️ **Live confirmation of why `synced` must never be trusted:** during this build the local subgraph froze at block 95309531 while the block ingestor kept pace with the chain head (~85 blocks behind), and graph-node reported **`synced: true`, `health: healthy`, `fatalError: null`** while **328,693 blocks behind**. Exactly the failure mode Stage 2 predicted. **The subgraph deployment is currently stalled - this is a live Stage 4 infra issue, not caused by #30, and nothing in this build diagnoses it.**

ℹ️ **One bug in NEW code that all 281 unit tests missed, also found by loading the page.** The new GraphQL client stored `fetch` bare as a class field, so `this.fetchImpl(...)` invoked it with `this` bound to the client instance - and the browser's `fetch` is hard-bound to `window`, so every real request died with `TypeError: Failed to execute 'fetch' on 'Window': Illegal invocation`. **Every test passed** because every test injects a mock, which has no `this` requirement: the default transport, the only path production takes, was never exercised. Fixed with an arrow wrapper, and `defaultFetchIsCallable` now covers the default explicitly (mutation-checked: restoring the bare reference fails it).

Suites: contracts **84/84** incl. fork, matchstick **13/13**, frontend **297/297** (was 196), `tsc -b` + `vite build` clean, bundle **653.62 kB / 204.28 kB gzip**. Mutation-checked: dropping `from` from the chart build fails 2, removing the viewport release fails 1, reverting either trade-feed empty branch fails 1 each, dropping the RPC timestamp fallback fails 2, restoring the bare `fetch` field fails 1.

**#29 shape (Build 16 - Stage 3, token page + swap page) - MERGED to `main` 2026-07-31** (fast-forward, no push). Reviewed on both axes before merging; the review commits are `ad75d8d` and `ba94ec7`.

- **The chart was not merely missing an x-axis - it was fabricating price history.** It plotted price against the trade *index* with monotone smoothing, so RDOGE's real shape (three buys inside six seconds, a 49-minute gap, one late buy, then hours of silence) rendered as a smooth evenly-paced S-curve.
- **recharts → TradingView Lightweight Charts** (Apache-2.0). Bundle **893 → 699 kB, 261 → 216 kB gzip**: recharts was 379 kB / 106 kB gzip, **42% of the whole JS bundle** for one chart. This front-loads part of build/30's trim.
- ⚠️ **Swapping the library did NOT by itself fix the lie, and this is the generalisable lesson.** Lightweight Charts' time scale is **ordinal** - one equal-width slot per data point, by design for OHLC bars - so raw events stayed evenly spaced (measured: pixel gaps 157/156/156/158). New `frontend/src/lib/priceSeries.ts` resamples onto a **fixed time grid with carry-forward prices**, which is what makes screen distance mean elapsed time, and is what a candle chart does anyway. For a curve whose marginal price is piecewise-constant, the carried value is exactly correct.
- A **graduated** curve stops dead rather than carrying a flat tail to now - the pool has moved on and this chart cannot see it.
- Token page: one "Graduated" card instead of two saying the same thing; `TradePanel` loses its dead graduated branch; the empty rail gains a per-token live trade feed with its own degraded state (Stage 2 split preserved). Swap page: new `PoolFacts` (spot price, fee tier, locked liquidity, pool link) read straight from RPC, so it survives an indexer outage; header no longer wraps. Both panels gain pay/receive legs.
- **Five defects were found by watching the running app, not by tests** - including `minMove: 1e-18` making the tick generator enumerate ~10⁸ steps so the chart **silently never painted** (no error at all; canvases just stayed at the 300×150 default), and the scroll-fade dimming the last row of any feed short enough to fit, which was **also live on the #28 board rail**.
- Suites after review: contracts 84/84 incl. fork, matchstick 13/13, frontend **196/196** (was 181, was 171), `tsc -b` + `vite build` clean, bundle ~700 kB / 216 kB gzip. Mutation-checked: pre-#29 duplicate-card shape fails 6, disconnecting `graduated` fails 1, removing the new viewport guard fails 2, forcing the rail's degraded branch fails 1.

**What the review changed, and the generalisable lesson in each.** The resampling decision itself **survived adversarial challenge and stands**. Three findings were the same class of lie the ticket exists to remove, which is the point worth remembering: fixing a chart's *spacing* does not by itself make the chart *honest*.

1. ⚠️ **The trade window was anchored to the WRONG END of history, and #29 turned that from a truncation into an assertion.** `TRADES_QUERY` ordered **ascending** under a 200-row cap, so past 200 trades a token page read the **oldest** 200 - and the new flat-to-`now` tail then carried that stale price forward, claiming the price had not moved when it had moved all day (measured: 200 trades ending at t=1199 with `now`=87600 produced an **86,401-second flat line**). Pre-#29 the line simply stopped and claimed nothing. Now `orderDirection: desc` with `fetchTrades` reversing back to ascending, so the **right edge is always real**, plus a "Last 200 trades" caption so the left edge cannot pass for the launch of the curve. **Invisible on testnet's sparse launches; certain on mainnet's 0.1s blocks.** The same cap had the per-token feed listing the *oldest* 200 trades under a "Live" header.
2. ⚠️ **The chart threw away the viewer's zoom every 5 seconds, so "zoom/pan work" was false.** `series` is rebuilt from the shared `useNowSeconds` clock and **one effect owned both the data and the framing**, so `setVisibleLogicalRange` re-fitted on every tick - measured 1 call at mount, 2 at 5s, 3 at 10s, and identically on a **graduated** curve whose data cannot change. Splitting the effect fixed the bug and the seam at once: **the data is ours and must track the chain; the viewport belongs to whoever is looking at it.** The chart follows new trades until the viewer touches it (wheel / pointer / touch), then leaves them alone.
3. **Markers sat one bucket LEFT of the step they caused.** A grid point carries every event at or before it, but markers snapped **down**, so a dot landed on the *pre-trade* price - at the foot of its own step, with the crosshair reporting a price that trade never made. Snap **up**, clamped to the final point. The old test only asserted markers land on times that *exist*, so this was untested; the tail guard is now relaxed by exactly one bucket, which is **forced rather than convenient** and equals the resolution the caption already states.
4. `PoolFacts` said **"Liquidity: locked forever" as a hardcoded literal**. `LPLock` holds *our* full-range NFT; a V3 pool also takes third-party liquidity that can be pulled at will. Relabelled **"Launch liquidity"**, which is the true claim, on the one surface where someone is deciding whether to trust the pool. It also had no error branch, so a failed `slot0` showed a placeholder forever and an unreadable pool collapsed to a confident `0`.
5. The rail on the **#28 board** asserted "the indexer is unreachable" for *any* failure of the trades query, even when the indexer's indexed head is level with the chain head. #29's own feed already diagnosed this correctly, so the two disagreed. Fixed on the board too. ℹ️ The first attempt read health *inside* `TradeRail` and broke twelve HomePage tests - **the design answering the question**: a presentational leaf should not open its own data subscription, so it takes `indexerState` as a prop like `TokenTradeFeed` does.
6. Dead CSS: `.input-amount` (orphaned by #29 when both panels moved to `.leg-amount`) plus pre-existing `.tag-buy`/`.tag-sell`. **Zero orphans remain** - re-scan with a grep of every class token in `styles.css` against `src/**/*.ts{,x}`.

⚠️ **One honesty limit is documented in `lib/priceSeries.ts` and deliberately NOT fixed.** Resampling keeps the **last** price in each bucket, so a move that fully reverses inside one bucket leaves **no trace**: verified, a 10x spike and return inside one hour of a 30-day span draws a **flat line**, and past `MAX_MARKERS` even the markers are gone. Nothing is fabricated - every plotted value genuinely held - but an excursion is *omitted*. Carrying each bucket's min and max is what a candle's wicks are for; that changes what the series **means**, so it was written down rather than smuggled into a review pass.

- **The range selector is still deliberately NOT built** (unrequested scope, → `build/30`). But the reasoning has changed and matters: resampling happens at **build time**, so **zoom cannot recover what the grid dropped** - magnifying a 1px cluster shows a flat plateau, not the burst. A selector that passes `from` into `buildPriceSeries` is the only mechanism that re-derives at usable resolution. The "just zoom" answer was never sufficient, and until finding #2 above it did not even work.

**#28 shape (Build 15 - Stage 3, the live board) - MERGED to `main` 2026-07-30:** The homepage is now a dense, board-first browse surface. **Flow changes, not paint:** live curves lead (graduations were a full row of cards *above* them, putting the finished thing above the actionable thing - now a ticker); a **sort control** (New / Closest / Volume / Busiest); a **cross-launch live trade rail**; and progress as the visual centre of a card, carrying a **heat colour** as well as a length so a grid of forty reads as a heat map before any digit is parsed. New pure modules `lib/board.ts` (sort + server `orderBy` mapping), `lib/heat.ts`, `lib/identicon.ts`; new `hooks/useArrivals.ts` and `hooks/useNowSeconds.ts`; `styles.css` rebuilt as a real token system (surfaces, type ramp, space scale, motion, focus, `prefers-reduced-motion`).

⚠️ **The sort had to become a SERVER-side `orderBy`, and this is the generalisable lesson.** The board is paged (`first: 50`) and the query hardcoded `orderBy: createdAtTimestamp`, so sorting the returned page client-side ranked only the newest 50: a curve at 95% older than that window could never appear under "Closest" - the one view where it matters most. `TOKENS_QUERY` now takes `$orderBy: Token_orderBy!`, verified against live graph-node for all four modes. `sortTokens` still runs, but only for the deterministic tiebreak that stops equal-keyed rows reshuffling every poll.

**Bugs found and fixed along the way:** every price on the board rendered as **scientific notation** (`formatPriceX18` falls back to exponential below 1e-6 and every launchpad price is below that, so `3.125e-11 ETH` was the most-repeated element on the page) - replaced with subscript-zero notation, plus a Unicode-subscript variant for the chart axis and tooltips, which had the same defect. The avatar **hid the `<img>` on error**, leaving a hole in the layout; since `metadataURI` is immutable a broken URI is permanent, so it now falls back to a letter tile coloured deterministically from the token **address** (not the symbol, which is attacker-chosen and duplicable). An **untraded** launch rendered as "0% to graduation / 0 ETH volume" beside an empty bar - three readings that together look like a broken card rather than a new one. A real-but-tiny position rendered as an empty track, indistinguishable from never-traded.

⚠️ **`contracts/script/SeedTestnet.s.sol` is new and is TESTNET-ONLY (hard-reverts on 4663).** A dense board cannot be designed or judged against one graduated token, so it seeds the **chain**, not fixtures. Buy amounts are derived from the curve's own invariant, so it stays correct if the calibration is re-scaled. `run()` builds the launch table; `churn()` refreshes recent trades and is safe to repeat. Testnet 46630 held **12 launches / 2 graduations** after this seeding (now **14**, after #30 added `ORICH` and `OPIN`), spread across the progress range and deliberately including the awkward cases (no metadata URI, a non-resolving `ipfs://` URI, an untraded launch). Commands in `docs/deployments-testnet.md`. ⚠️ Note `--slow` is required: without it a buy can be submitted before the creation it depends on.

ℹ️ **One bug was found by watching the page, not the tests, and the tests would never have caught it:** `useArrivals` adopted its baseline on the first effect run, which happens while the query is still loading and the list is empty - so the entire first page counted as arrivals and 23 rows flashed at once, the exact glitch the hook exists to prevent. Fixed to adopt from the first *non-empty* list, and verified end to end by running `churn()` against live testnet (0 arrivals on load, 3 after new trades landed). Suites: contracts 84/84 incl. fork, frontend **149/149** (was 103), `tsc -b` + `vite build` clean. Both page-level assertions were mutation-checked - disconnecting the sort fails 3 tests, reinstating graduated cards in the board fails 6.

**#27 shape (Build 14 - Stage 4, RPC failover + provider survey) - MERGED to `main` 2026-07-30:** The frontend takes an **ordered endpoint list** instead of one URL. New `frontend/src/lib/rpcEndpoints.ts` owns the policy as a pure function (`VITE_RPC_URL`, then `VITE_RPC_URL_2`, then the chain's public endpoint, deduplicated, never empty); `lib/wagmi.ts` feeds it to viem's `fallback` with **ranking off** (the last entry is always the rate-limited public endpoint, and a latency probe would promote it exactly when the paid primary is busy). `FALLBACK_OPTIONS` is exported so tests exercise the **shipped** options. `rpcFailover.test.ts` pins viem's real semantics **and the wiring** - without the wiring assertion, swapping `fallback` back to a single `http()` passed the entire suite (mutation-checked). **Sourcing verdict: no usable keyless second endpoint exists; it needs an account, and Alchemy is the pick.** `scripts/rpc-probe.mjs` gained **adaptive pacing** (`--min-interval`, plus a floor that self-raises on throttling) and now reports **THROTTLED distinctly from FATAL** - measurement trap #4, a throttle misread as an incapability, which had already caused the harness to write off a working candidate. New **`--self-test`** runs 18 reply-classification cases offline (no network, no deps); all four classifier mutants are caught. Two bugs the review caught: a bare `429` in the rate-limit pattern misread results like `0x429ab1` as refusals, and treating any HTML as throttling made a typo'd URL report "slow down" after a 4-minute retry ladder (now 0.9 s and "not a JSON-RPC endpoint"). Also fixed a pre-existing gap: `.gitignore`'s `.env.*` had made `frontend/.env.example` **untracked**, so the README's "copy `.env.example`" step was impossible on a fresh clone. Suites: contracts 84/84, matchstick 13/13, frontend **103/103** (was 84), `tsc -b` + `vite build` clean.

**#26 shape (Build 13 - Stage 4, mainnet RPC capability) - MERGED to `main` 2026-07-30:** `scripts/rpc-probe.mjs` (dependency-free Node, **strictly read-only**, takes `mainnet` / `testnet` / any URL, plus `--json`) and findings in **`docs/rpc-capability.md`**. Because it accepts any URL it is also the evaluation harness for candidate providers. **Verdict: the "Postgres is a derived cache" assumption HOLDS on mainnet** - `eth_getLogs` serves from block 0 with no pruning, proven with *unfiltered* windows at 11 depths (a filtered empty result cannot distinguish an idle contract from a pruned node). Suites re-run green: contracts 84/84 incl. fork, frontend 84/84. The probe's own design lessons are written up in the doc because each one first produced a confidently wrong number: measure archive depth with `eth_call` not `eth_getBalance` (the latter returns `0x0` with **no error** on unreadable state); never characterise a load-balanced endpoint from a single sample; and never treat a result-cap refusal as evidence of pruning, since it actually *proves* logs exist there.

**#25 shape (Build 12 — Stage 2, RPC-first trading):** The trade path is fully decoupled from the indexer. New `hooks/useOnchainToken.ts` resolves curve, graduation state, pool and labels from RPC alone, rooted at `VITE_FACTORY_ADDRESS`; `lib/onchainToken.ts` holds the decision table as a pure function (15 unit tests) so the money path's states are testable without a chain. New `hooks/useIndexerStatus.ts` + `lib/indexerHealth.ts` measure real lag (`_meta.block.timestamp` vs RPC head timestamp — never `synced`) and drive a global `IndexerBannerView` plus per-panel `IndexedDataNotice`. `SwapPage` and `TokenPage` no longer read any address from the subgraph; `lib/address.ts` parses route params case-insensitively (viem's strict EIP-55 check was rejecting valid addresses). Multicall3 declared on both chains. **Validated by stopping graph-node against the live testnet**: swap page still resolved pool `0xDC27…E933` and rendered the panel, with "Charts unavailable / Trading is unaffected"; token page kept its trade panel with three individually-labelled degraded panels; WETH9 correctly rejected as not-a-launch; everything recovered on restart. 79 frontend tests green.

**#20/#21 shape (Builds 09/10 — frontend):** Custom React + Vite + TS SPA in `frontend/` (spec decision #8 — **not** a `Uniswap/interface` fork). wagmi/viem (injected connector only), react-query with ~5s live polling, graphql-request against the #19 subgraph, recharts. **#20:** create-token form (`createLaunch(name, symbol, metadataURI)`; the URI goes on-chain and is permanent, "bring your own URI" for v1 — the local image field is now only a preview), live curve view (chart + progress from subgraph, buy/sell quoted on-chain via `quoteBuy`/`quoteSell`, anti-snipe cap from cumulative `purchasedOf`), transparent creator/holder positions. **#21:** "just graduated" feed + live-curves browse, and a swap page (`/swap/:address`) trading graduated pools through the platform's **own v3-periphery `SwapRouter`** — ETH→TOKEN via payable `exactInputSingle`, TOKEN→ETH via `multicall([exactInputSingle → router, unwrapWETH9 → user])`; output now comes from our own `QuoterV2` (exact, incl. fee + price impact) with on-chain `amountOutMinimum` protection, falling back to a labelled `slot0` spot estimate when `VITE_QUOTER_ADDRESS` is unset. **43 unit tests green** (`cd frontend && npm test`), `tsc -b` + `vite build` clean. Env-driven addresses (`VITE_FACTORY_ADDRESS` / `VITE_SWAP_ROUTER_ADDRESS` / `VITE_SUBGRAPH_URL`); runs in preview mode with a banner until set. Both reviews applied per ticket. Bundle is ~883 kB (wagmi pulls unused WalletConnect/MetaMask SDK — a `manualChunks`/trim follow-up); token metadata is now on-chain and global (#24); what remains for Stage 3 is the READ side — an IPFS gateway choice, a fallback avatar for unpinned URIs, and a moderation denylist. **Superseded in part by #25**: the pages described here no longer read any trade-path address from the subgraph. **And in part by #29**: recharts is gone (replaced by TradingView Lightweight Charts), the ~883 kB bundle figure is stale (now ~699 kB / 216 kB gzip), and the fallback avatar landed in #28 - so what actually remains for the metadata read side is the IPFS gateway choice and the moderation denylist.

**#19 shape (Build 08 — subgraph):** The Graph subgraph in `subgraph/` (graph-node toolchain) indexing `eip155:4663`. Sources: `LaunchpadFactory.LaunchCreated` (fixed addr) → `Token` + spawns a **`BondingCurve` data-source template** per launch (curve addresses are runtime-discovered; token address passed via data-source context); `BondingCurve.Bought/Sold/Graduation` (template) → `Trade`/`Holder`/curve-progress; `GraduationManager.Graduated` (fixed addr) → the `Graduation` feed. Entities: `Factory` (rollups), `Token` (curve progress: reserves, tokensSold, priceX18, progressBps; plus `metadataURI` and the six frozen curve params from #24), `Trade` (immutable buy/sell log), `Holder` (netted on-curve position), `Graduation` (just-graduated feed). All derived from events, **no eth_calls** (since #24 even `curveTokenAllocation` arrives in `LaunchCreated`, so no Solidity constant is copied into the mappings) → deterministic + matchstick-testable. **13 matchstick tests green** (`cd subgraph && npm test`; needs `libpq` on macOS — see `subgraph/README.md`). Both reviews applied: fixed a `volumeEth` overcount on the graduation-crossing buy (was counting the refunded ETH). **Not deployed** — self-hosted graph-node (chain 4663 isn't on The Graph's hosted/decentralized nets); manifest carries placeholder addresses pending #18 deploy keys, filled via `networks.json` (`graph build --network robinhood`). v1 holders are curve-position-only; secondary ERC-20 transfers are a documented follow-up.

**#18 shape:** `LaunchpadFactory` is now `Ownable2Step` (two-step handoff to a Safe multisig — a mistyped owner can't brick control). Added guarded `setCurveParams` (virtualEthReserve / tradeFeeBps ≤10% / maxBuyPerWallet / antiSnipeThreshold ≤800M), future-only; `virtualTokenReserve` stays calibration-locked so graduation price continuity (#16) holds for any V_eth. Full deploy pipeline in `contracts/script/DeployLaunchpad.s.sol` + runbook `docs/deploy.md` (testnet 46630 → mainnet 4663, Blockscout verify). Real on-chain broadcast + Blockscout verification need deploy keys (not available in-session); delivered as production scripts + runbook, validated via fork dry-run + a fork test mirroring the ownership choreography.

**Optional low-severity test gaps left on #17** (flagged in review, non-blocking): negative-owner tests for `setProtocolFee`/`setPoolProtocolFee`; an explicit `NPM.burn` reverts assertion; a `ProtocolFeeSkipped`-path test.
